# Ejercicio 2 - Crear tabla EMPLEADO

**Crea una tabla "EMPLEADO" con campos id, nombre y salario.**

La tabla creamos con una consulta sql, como en sqldeveloper, y creando un Statement ejecutamos la actualizacion (Statement.executeUpdate()), para crear la tabla.
